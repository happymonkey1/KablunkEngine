#include "kablunkpch.h"

#include "Kablunk/Asset/AssetRegistry.h"

namespace Kablunk::asset
{

	void AssetRegistry::serialize(const std::filesystem::path& filepath) const
	{
		KB_CORE_ASSERT(false, "not implemented!");
	}

	void AssetRegistry::serialize_binary(const std::filesystem::path& filepath) const
	{
		KB_CORE_ASSERT(false, "not implemented!");
	}

	bool AssetRegistry::deserialize(const std::filesystem::path& filepath)
	{
		KB_CORE_ASSERT(false, "not implemented!");
	}

	bool AssetRegistry::deserialize_binary(const std::filesystem::path& filepath)
	{
		KB_CORE_ASSERT(false, "not implemented!");
	}

}
